{
    "apps": [
        {
            "name": "shadowsocks-manager",
            "script": "server.js",
            "cwd": "APPCWD",
            "watch": true,
            "exec_mode": "cluster",
            "max_memory_restart": "512M",
            "args": "-c CONF_PATH/shadowsocks-libev.yml"
        }
    ]
}
